# Local-only Claude context (kept on this machine, never pushed)
notes_temp
.claude
CLAUDE.md
# Python
__pycache__/
*.py[cod]
*.so
.venv/
venv/
dist/
*.egg-info/
.pytest_cache/

# Node
node_modules/
ui/dist/
ui/.vite/

# Env
.env
.env.*
!.env.example

# Logs
*.log

# Data (runtime, not source)
data/sqlite/*.db
data/postgres/
data/minio/
data/redis/
data/temp/
data/portals/
data/martin/martin-config.yaml

# Certs
data/certbot/

# IDEs
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Local-only working directory: reference clones and anything else not part of GeoDeploy.
# One recursive entry ON PURPOSE — naming the individual clones here would put them in a public
# file, which is the opposite of keeping them local.
vendor/

# Scratch portal-config dumps written by examples/geodeploy_cli.py (portal-get)
examples/*.json
examples/.venv/
