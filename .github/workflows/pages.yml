name: Docs

# Builds the documentation site and publishes it to GitHub Pages.
# Pull requests build but do NOT deploy, so a broken docs change is caught before it is live.
on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
      - 'requirements-docs.txt'
      - '.github/workflows/pages.yml'
  pull_request:
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
      - 'requirements-docs.txt'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Never let two deploys race; the newer one wins.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Turns Pages on for the repository if it is not already, and reports the site URL. Without
      # this (or the manual Settings -> Pages -> Source: GitHub Actions toggle) the BUILD succeeds and
      # the DEPLOY fails with a bare 404, because there is no Pages site to deploy into.
      # Only meaningful on a real push — a fork or a PR from one cannot enable Pages.
      - uses: actions/configure-pages@v5
        if: github.event_name != 'pull_request'
        with:
          enablement: true
        continue-on-error: true
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: requirements-docs.txt
      - run: python -m pip install -r requirements-docs.txt
      # --strict turns broken internal links and bad nav entries into build failures. A docs site
      # whose links quietly rot is worse than one that refuses to build.
      - run: mkdocs build --strict
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
