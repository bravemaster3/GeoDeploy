{
  "name": "geodeploy-publish-plugin",
  "version": "0.1.0",
  "description": "GeoLibre plugin: publish the current project to a GeoDeploy instance as a hosted portal.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "vite build --config vite.geolibre.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
